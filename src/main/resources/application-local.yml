server:
  port: 8086
spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:mysql://120.26.57.239:3306/arknights?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: wz000too
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapperLocations: classpath*:/mapper/*.xml
  configuration:
    mapUnderscoreToCamelCase: true
scheduled:
  # 每天晚8点日报统计
  countJob: 0 0 20 */1 * ?
  # 每天早8点播报干员庆生
  birthdayJob: 0 0 8 */1 * ?
  # 每3分钟监听一次B站动态
  biliJob: 0 */3 * * * ?
  # 每3分钟监听一次微博动态
  sinaJob: 0 */3 * * * ?
  # 每天9点检查一次数据更新
  updateJob: 0 0 9 */1 * ?
  # 每周日晚5点剿灭提醒
  cleanJob: 0 0 17 ? * SUN
userConfig:
  #登录的机器人qq
  loginQq: 471690273
  #OPQ框架部署的IP
  OPQUrl: 120.26.57.239
baiduConfig:
  APP_ID:
  API_KEY:
  SECRET_KEY: